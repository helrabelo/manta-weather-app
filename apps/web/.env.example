# Base URL for the API. In development, Vite proxies /api to the local worker.
# Only set this if the API is hosted at a different origin (e.g. production).
# VITE_API_URL=https://manta-weather-app.your-subdomain.workers.dev
